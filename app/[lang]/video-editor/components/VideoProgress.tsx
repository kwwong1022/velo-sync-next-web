import { Locale } from "@/i18n.config"
import { getDictionary } from "@/lib/dictionaries"

type Props = { lang: Locale }

export default function VideoProgress({ lang }: Props) {
    return (
        <div>
            Video Progress
        </div>
    )
}